# PostgreSQLの接続先（必須）
DATABASE_URL="postgres://user:password@localhost:5432/game?sslmode=disable"

# HTTPサーバーの待ち受けポート（未設定時は8080）
PORT=8080

# CORSで許可するオリジンをカンマ区切りで指定（未設定時は下記と同じ値がデフォルト）
CORS_ALLOW_ORIGINS="http://localhost:3000,http://localhost:5173"
